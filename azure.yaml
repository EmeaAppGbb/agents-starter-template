# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: agents-starter-template
services:  
  backend:
    language: dotnet
    project: ./src/api/Agents.Api.csproj
    host: containerapp
  frontend:
    language: node
    project: ./src/frontend
    host: containerapp